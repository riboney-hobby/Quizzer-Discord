// const bot = require('../src/discord/bot')
// const config = require('../src/shared/configs')

test('Success: Discord bot starts up', async () => {
    // await bot.start(config.BOT_TOKEN, config.BOT_CLIENT_ID, config.GUILD_ID)
    expect(5).toBe(5)
})
